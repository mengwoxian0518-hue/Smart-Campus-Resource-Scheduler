<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.campus.mapper.AdminMapper">
    <select id="listStaff" resultType="com.campus.entity.Staff">
        select * from staff
        <where>
            <!-- 名字：模糊查询，使用 concat 防止 SQL 注入 -->
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <!-- 职责：精确匹配 -->
            <if test="jobTitle != null and jobTitle != ''">
                and job_title = #{jobTitle}
            </if>
            <!-- 入职时间范围：灵活处理，可以只传开始或只传结束 -->
            <if test="beginTime != null">
                and hire_date &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and hire_date &lt;= #{endTime}
            </if>
        </where>
        order by create_time desc
    </select>

</mapper>
