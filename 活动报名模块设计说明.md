## 1. 目标

新增“校园活动报名”能力，覆盖三端：

- 管理端（Vue）：活动的增删改查、上下线/状态展示
- 后端（Spring Boot）：管理员活动 CRUD 接口、用户活动查询与报名接口
- 小程序：活动列表/详情/报名入口、我的已参加活动列表

同时满足：

- 活动信息缓存到 Redis，减少数据库压力
- 报名高并发场景下：一人只能报名一次、防止超卖
- 报名写库异步化：Lua 原子校验 + Redis Stream 消息 + 后台消费入库

---

## 2. 数据模型（MySQL）

### 2.1 activity（活动表）

- `facility_id`：关联场所（facility 表）
- 活动时间：`start_time` ~ `end_time`
- 报名窗口：`signup_start_time` ~ `signup_end_time`
- 人数：`max_participants`、`current_participants`
- 状态：`status`（0 已关闭、1 报名中、2 已满额、3 已结束）

建议补充/约定：

- `current_participants` 只做展示冗余字段，写库时以“成功报名”增量维护；必要时可用 SQL count 校准
- 报名窗口外不允许报名；活动已结束/关闭不允许报名

### 2.2 activity_signup（报名表）

- 唯一约束：`uk_user_activity(user_id, activity_id)`，保证数据库层幂等

---

## 3. 后端接口设计

### 3.1 管理端接口（admin）

建议统一前缀：`/admin/activity`

- `POST /admin/activity`：新增活动
- `PUT /admin/activity`：修改活动
- `DELETE /admin/activity`：批量删除（ids）
- `GET /admin/activity/page`：分页查询（name、status、时间范围等可选）
- `GET /admin/activity/{id}`：详情

返回 VO 建议带：

- 活动字段 + `facilityName`（join facility）

缓存策略（见第 5 节）：

- 新增/修改/删除后，清理相关活动缓存（列表/详情）

### 3.2 用户端接口（user）

建议统一前缀：`/user/activity`

- `GET /user/activity/page`：活动分页（可按“可报名/已结束”等筛选）
- `GET /user/activity/{id}`：活动详情
- `POST /user/activity/{id}/signup`：报名（走 Lua + Stream 异步）
- `GET /user/activity/my`：我已参加的活动（从报名表查，join 活动信息）

报名规则：

- 当前时间必须在 `signup_start_time` ~ `signup_end_time` 内
- 活动必须是可报名状态（通常为 1），且未满额

---

## 4. Redis 设计

### 4.1 活动信息缓存

缓存目标：降低“列表/详情”查询 DB 压力。

Key 设计（示例）：

- 活动详情：
  - `activity:detail:{activityId}` -> JSON（ActivityDetailVO），TTL 10~30 分钟
- 活动列表分页：
  - `activity:page:{hash(params)}` -> JSON（PageResult<ActivityVO>），TTL 1~5 分钟

失效策略：

- 管理端 CRUD 成功后：删除对应详情 key；列表可用“版本号”或“批量前缀删除”的方式处理（推荐：列表 key 内加入版本号）
- 也可以只缓存“详情”，列表走 DB（实现更简单），视数据量和压力决定

### 4.2 报名库存与用户幂等缓存（高并发核心）

Key 设计（示例）：

- 活动剩余名额（库存）：
  - `activity:stock:{activityId}` -> int（剩余名额）
  - 初始化：`max_participants - current_participants`
- 已报名用户集合：
  - `activity:signup:users:{activityId}` -> Set(userId)
- 报名消息流：
  - `stream:activity:signup`（Redis Stream）
  - 消费组：`activity_signup_group`
  - 消费者：`consumer-{instanceId}`

缓存预热：

- 活动上架/新增时：写入 stock 与 users set（users 可不预热，按需写）
- 系统启动时：批量加载“报名中”的活动到 Redis（可选，按规模）

---

## 5. Lua 原子脚本：1 秒内完成校验 + 扣减 + 投递消息

目标：在 Redis 内一次性完成：

- 校验该用户是否已报名
- 校验库存是否充足
- 扣减库存并记录用户已报名
- 将报名事件写入 Stream 供异步入库

### 5.1 Lua 入参约定（建议）

Keys：

- `KEYS[1]` = `activity:stock:{activityId}`
- `KEYS[2]` = `activity:signup:users:{activityId}`
- `KEYS[3]` = `stream:activity:signup`

Args：

- `ARGV[1]` = activityId
- `ARGV[2]` = userId
- `ARGV[3]` = signupTime（或由消费端生成）

### 5.2 Lua 返回值

- `1`：报名成功（已扣减库存并写入 stream）
- `0`：报名失败（库存不足 或 用户已报名）

### 5.3 关键点

- “一人一次”：用 Set `SISMEMBER` 判断，成功时 `SADD`
- “防超卖”：用库存 key 扣减，扣减前判断 `stock > 0`
- “写消息”：用 `XADD stream:activity:signup * activityId ... userId ...`

---

## 6. 异步入库：Redis Stream 消费组 + 后台线程

### 6.1 消费流程

后台线程循环：

1. `XREADGROUP GROUP activity_signup_group consumer-1 COUNT N BLOCK 2000 STREAMS stream:activity:signup >`
2. 拿到消息后，写入 `activity_signup`：
   - 插入（activity_id, user_id）
   - 同时更新 activity：
     - `current_participants = current_participants + 1`
     - 若达到 max，则 status 置为 2（已满额）
3. 成功后 `XACK` ack

### 6.2 幂等与异常处理

- 数据库唯一约束兜底：若插入报名表触发重复键，按“已报名”处理，直接 ack（避免反复消费）
- 消费失败不 ack：消息会留在 Pending List，可由定时任务或同线程补偿处理
- 若需要严格一致性（库存已扣但 DB 未写）：可增加“补偿机制”
  - 例如：超过一定时间仍在 Pending 的消息，重试入库；若确认失败可回补库存（复杂度更高）

---

## 7. 管理端（Vue）改造点

新增“活动管理”模块：

- 路由：`/activity`（或放在现有管理菜单结构下）
- 页面能力：
  - 列表：分页、按状态筛选、按活动时间/报名窗口筛选
  - 新增/编辑：表单包含场所选择（facility 下拉）、时间选择器、人数上限等
  - 删除：支持批量

接口调用：

- 使用现有 axios 封装风格（与 resource/facility 模块保持一致）

---

## 8. 小程序改造点

新增入口：

- 活动列表页（展示报名中/已结束等状态）
- 活动详情页（展示活动信息、报名窗口、剩余名额/是否已报名）
- 我的活动页（展示已报名活动列表）

报名按钮规则：

- 只有在报名窗口内、且活动状态允许报名时显示“报名”
- 否则显示“未到报名时间/报名已结束/已满额/已关闭/已结束”

---

## 9. 实施顺序（建议）

1. 后端：新增 entity/mapper/xml/service/controller（活动 CRUD + 用户查询）
2. Redis：活动缓存（详情/列表）与失效策略
3. 高并发报名：Lua + Stream + 消费线程（先跑通链路，再做补偿与完善）
4. 管理端：活动管理页面对接
5. 小程序：活动入口、报名、我的活动

---

## 10. 你检查时重点关注的点（验收清单）

- 接口与字段命名是否符合你项目现有风格
- 报名窗口校验（后端强校验 + 前端提示）
- Lua 是否满足：原子校验 + 扣减 + 防重复 + 投递消息
- Stream 消费是否幂等（重复消息、重复消费、不丢单）
- 缓存失效策略是否可接受（CRUD 后刷新/删除缓存）

